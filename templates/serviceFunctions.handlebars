/**
* This file was generated by OpenApi Typescript Sync cli tool.
*
* Do not edit this file as it might be regenerated.
*/


// eslint-disable-next-line @typescript-eslint/ban-ts-ignore
// @ts-ignore
import Axios, {AxiosRequestConfig, AxiosResponse} from 'axios';
{{#each schemas}}
import { {{.}} } from '../schemas/{{.}}';
{{/each}}
{{{config.options.apiUrlConstant.import}}}

/**
* {{summary}}{{#if description}}
* {{description}}{{/if}}
*/
export const {{name}} = {
{{#each routes}}
	{{#each .}}
	/**
	 * {{summary}}{{#if description}}
	 * {{description}}{{/if}}
	 */
	async {{camel_case summary}}: ({{getParameters .}}): Promise<AxiosResponse<{{getAxiosResponseType .}}>> => {
		return Axios.{{@key}}<{{getAxiosResponseType .}}>(`{{{getRoute .}}}`{{#if requestBody}}, data{{/if}}, config);
	},

    {{/each}}
{{/each}}
};
